# Which model provider to use: "anthropic" or "stub".
# Leave empty to use Claude when ANTHROPIC_API_KEY is set, and the offline stub otherwise.
LLM_PROVIDER=

# Anthropic API key. Only needed for the Claude provider.
ANTHROPIC_API_KEY=your-anthropic-api-key

# Claude model id.
ANTHROPIC_MODEL=claude-opus-5
